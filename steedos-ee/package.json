{
  "name": "project-ee",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/**"
  ],
  "scripts": {
    "start": "steedos start",
    "bootstrap": "lerna clean --yes && lerna bootstrap --force-local",
    "build": "yarn bootstrap && lerna run build"
  },
  "dependencies": {
    "@steedos-builder/react": "0.2.30",
    "@steedos/service-enterprise": "2.3.2-beta.12"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
