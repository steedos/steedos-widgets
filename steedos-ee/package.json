{
  "name": "project-ee",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "examples/*",
    "packages/*",
    "services/*",
    "ee/*",
    "server",
    "steedos-packages/*"
  ],
  "scripts": {
    "start": "steedos start",
    "bootstrap": "lerna clean --yes && lerna bootstrap --force-local",
    "build": "yarn bootstrap && lerna run build"
  },
  "dependencies": {
    "@steedos/service-enterprise": "2.3.0-beta.8"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
